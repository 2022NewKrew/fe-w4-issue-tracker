{
  "presets": [
    ["@babel/preset-react", { "runtime": "automatic" }],
    [
      "@emotion/babel-preset-css-prop",
      {
        "autoLabel": "dev-only",
        "labelFormat": "[dirname]-[filename]-[local]"
      }
    ]
  ]
}
