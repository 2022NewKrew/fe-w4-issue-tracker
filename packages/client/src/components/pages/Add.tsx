import AppLayout from "@templetes/AppLayout";
import { IssueForm } from "@UI/Organisms";

const Add = () => {
  return (
    <AppLayout>
      <IssueForm />
    </AppLayout>
  );
};

export default Add;
