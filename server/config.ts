const dbConfig = {
  host: "127.0.0.1",
  user: "root",
  password: "",
  database: "issue",
  waitForConnections: true,
  connectionLimit: 10,
};
const jwtSecret = "kakaoonboarding";
export { dbConfig, jwtSecret };
