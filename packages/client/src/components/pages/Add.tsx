import AppLayout from "@templetes/AppLayout";
import { IssueRegisterForm } from "@UI/Organisms";

const Add = () => {
  return (
    <AppLayout>
      <IssueRegisterForm />
    </AppLayout>
  );
};

export default Add;
